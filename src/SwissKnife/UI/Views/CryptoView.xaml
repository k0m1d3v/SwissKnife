<UserControl x:Class="SwissKnife.UI.Views.CryptoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="520" d:DesignWidth="760">
    <ScrollViewer VerticalScrollBarVisibility="Auto" 
                  HorizontalScrollBarVisibility="Disabled"
                  Padding="0 0 6 0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Card 1: Calcola Hash -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Hash SHA-256" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <Border Background="{DynamicResource AccentBrush}" CornerRadius="8" Padding="8 4" Margin="10 0 0 0">
                            <TextBlock Text="Crypto" FontWeight="Bold" Foreground="White" FontSize="12"/>
                        </Border>
                    </StackPanel>
                    <TextBlock Text="Calcola l'hash SHA-256 di un file usando streaming e progressione." Foreground="{DynamicResource TextSecondaryBrush}" Margin="0 12 0 0" />

                    <StackPanel Margin="0 12 0 0">
                        <TextBlock Text="File da elaborare" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" Margin="0 0 0 4"/>
                        <Grid Margin="0 0 0 10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="FilePathTextBox" MinWidth="400" VerticalContentAlignment="Center"/>
                            <Button Grid.Column="1" Content="Sfoglia" Style="{StaticResource GhostButtonStyle}" Click="BrowseButton_Click" Margin="10 0 0 0" />
                        </Grid>

                        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                            <Button x:Name="CalculateButton" Content="Calcola SHA-256" Style="{StaticResource PrimaryButtonStyle}" Click="CalculateButton_Click" Width="160"/>
                            <Button x:Name="CancelButton" Content="Annulla" Style="{StaticResource GhostButtonStyle}" Click="CancelButton_Click" Width="100" IsEnabled="False" Margin="8 0 0 0"/>
                        </StackPanel>

                        <StackPanel Margin="0 0 0 10">
                            <TextBlock Text="Avanzamento" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
                            <ProgressBar x:Name="OperationProgressBar" Minimum="0" Maximum="100" Visibility="Collapsed" Margin="0 6 0 0"/>
                        </StackPanel>

                        <Border Background="{DynamicResource ResultBackgroundBrush}" BorderBrush="{DynamicResource BorderBrushLight}" BorderThickness="1" CornerRadius="10" Padding="12">
                            <Grid VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Risultato" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
                                    <TextBox x:Name="HashOutputTextBox" IsReadOnly="True" VerticalContentAlignment="Center" Margin="0 4 0 0"/>
                                </StackPanel>
                                <Button Grid.Column="1" x:Name="CopyButton" Content="Copia" Style="{StaticResource GhostButtonStyle}" Width="90" Click="CopyButton_Click" IsEnabled="False" Margin="10 0 0 0"/>
                            </Grid>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Card 2: Verifica Hash -->
            <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0 12 0 0">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Verifica Hash SHA-256" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <Border Background="{DynamicResource SuccessBadgeBrush}" CornerRadius="8" Padding="8 4" Margin="10 0 0 0">
                            <TextBlock Text="Verifica" FontWeight="Bold" Foreground="White" FontSize="12"/>
                        </Border>
                    </StackPanel>
                    <TextBlock Text="Verifica se un hash SHA-256 corrisponde a un file." Foreground="{DynamicResource TextSecondaryBrush}" Margin="0 12 0 0" />

                    <StackPanel Margin="0 12 0 0">
                        <TextBlock Text="File da verificare" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" Margin="0 0 0 4"/>
                        <Grid Margin="0 0 0 10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="VerifyFilePathTextBox" MinWidth="400" VerticalContentAlignment="Center"/>
                            <Button Grid.Column="1" Content="Sfoglia" Style="{StaticResource GhostButtonStyle}" Click="VerifyBrowseButton_Click" Margin="10 0 0 0" />
                        </Grid>

                        <TextBlock Text="Hash SHA-256 atteso" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" Margin="0 0 0 4"/>
                        <Grid Margin="0 0 0 10">
                            <TextBox x:Name="ExpectedHashTextBox" VerticalContentAlignment="Center"/>
                        </Grid>

                        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                            <Button x:Name="VerifyButton" Content="Verifica Hash" Style="{StaticResource PrimaryButtonStyle}" Click="VerifyButton_Click" Width="160"/>
                            <Button x:Name="VerifyCancelButton" Content="Annulla" Style="{StaticResource GhostButtonStyle}" Click="VerifyCancelButton_Click" Width="100" IsEnabled="False" Margin="8 0 0 0"/>
                        </StackPanel>

                        <StackPanel Margin="0 0 0 10">
                            <TextBlock Text="Avanzamento" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
                            <ProgressBar x:Name="VerifyProgressBar" Minimum="0" Maximum="100" Visibility="Collapsed" Margin="0 6 0 0"/>
                        </StackPanel>

                        <Border x:Name="VerifyResultBorder" Background="{DynamicResource ResultBackgroundBrush}" BorderBrush="{DynamicResource BorderBrushLight}" BorderThickness="1" CornerRadius="10" Padding="12" Visibility="Collapsed">
                            <StackPanel>
                                <TextBlock x:Name="VerifyResultTextBlock" FontSize="14" FontWeight="SemiBold"/>
                                <TextBlock x:Name="VerifyDetailTextBlock" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" Margin="0 4 0 0" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Card 3: Log -->
            <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0 12 0 0" Padding="14">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Log" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Text="(ultimi messaggi)" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" Margin="8 0 0 0"/>
                    </StackPanel>
                    <TextBox x:Name="LogTextBox"
                             Height="120"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             Margin="0 6 0 0"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
